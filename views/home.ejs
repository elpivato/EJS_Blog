<%- include('partials/header'); -%>

<h1>Home</h1>
<p><%= homeString %></p>

<% posts.forEach(function(post) {%>
   <h1> <%= post.title; %> </h1>
   <p> <%= post.text; %> </p>
    <% }) %>

<%- include('partials/footer'); -%>